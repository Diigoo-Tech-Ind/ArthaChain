# ArthaChain Cloudflare Tunnel Configuration
# Updated with Advanced ArthaChain APIs
# Main Node: Port 1900 (includes all ArthaChain APIs)

tunnel: arthachain-testnet
credentials-file: /Users/sainathtangallapalli/.cloudflared/cc3311b0-2a5c-4444-aacc-668576634499.json

# Ingress rules for routing traffic
ingress:
  # Main Blockchain Node with Advanced ArthaChain APIs (Port 1900)
  - hostname: testnet.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Advanced APIs (Port 1900 - same as main node)
  - hostname: api.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Consensus APIs
  - hostname: consensus.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain DAG Processing APIs
  - hostname: dag.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain AI-Native APIs
  - hostname: ai.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Quantum Resistance APIs
  - hostname: quantum.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Self-Healing APIs
  - hostname: selfhealing.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Dynamic Roles APIs
  - hostname: roles.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Cross-Chain Bridge APIs
  - hostname: bridges.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Mobile Optimization APIs
  - hostname: mobile.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # ArthaChain Enterprise APIs
  - hostname: enterprise.arthachain.in
    service: http://localhost:1900
    originRequest:
      connectTimeout: 30s
      readTimeout: 30s
      writeTimeout: 30s
      noTLSVerify: false
    headers:
      - name: X-Forwarded-For
        value: "{{ .ClientIP }}"
      - name: X-Real-IP
        value: "{{ .ClientIP }}"
      - name: X-Forwarded-Proto
        value: "https"

  # Catch-all rule for unmatched hostnames
  - service: http_status:404
